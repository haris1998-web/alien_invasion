class DinoSettings:
    """A class to store all settings for game_character."""
    def __init__(self):
        """Initialize the game characters settings."""
        # Screen settings
        self.screen_width = 1200
        self.screen_height = 800
        self.bg_color = (0, 0, 0)
